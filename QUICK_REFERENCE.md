/**\n * QUICK REFERENCE GUIDE\n * Copy & paste snippets for common tasks\n */\n\n// ============================================\n// 1. HOW TO USE CART STATE\n// ============================================\n\n// In any component:\nimport { useCart } from '../state/CartContext';\n\nconst MyComponent = () => {\n  const { state, addToCart, removeFromCart, updateQuantity, clearCart } = useCart();\n  \n  // Access cart data\n  console.log(state.items);      // Array of cart items\n  console.log(state.totalPrice);  // Total price\n  console.log(state.totalItems);  // Item count\n  \n  // Add item to cart\n  addToCart({\n    id: '123',\n    name: 'Product',\n    price: 100,\n    quantity: 1,\n    // ... other fields\n  });\n  \n  // Remove from cart\n  removeFromCart('product-id');\n  \n  // Update quantity\n  updateQuantity('product-id', 5);\n  \n  // Clear entire cart\n  clearCart();\n};\n\n// ============================================\n// 2. HOW TO USE FAVORITES STATE\n// ============================================\n\nimport { useFavorites } from '../state/FavoritesContext';\n\nconst MyComponent = () => {\n  const { items, addToFavorites, removeFromFavorites, isFavorite } = useFavorites();\n  \n  // Check if product is favorited\n  const liked = isFavorite('product-id');\n  \n  // Add to favorites\n  addToFavorites(productObject);\n  \n  // Remove from favorites\n  removeFromFavorites('product-id');\n  \n  // List all favorites\n  items.forEach(product => console.log(product.name));\n};\n\n// ============================================\n// 3. HOW TO USE USER STATE\n// ============================================\n\nimport { useUser } from '../state/UserContext';\n\nconst MyComponent = () => {\n  const {\n    user,\n    isAuthenticated,\n    loginSuccess,\n    logout,\n    updateUser,\n    addAddress,\n    removeAddress,\n  } = useUser();\n  \n  // Check if logged in\n  if (!isAuthenticated) {\n    return <LoginScreen />;\n  }\n  \n  // Access user data\n  console.log(user?.name);\n  console.log(user?.email);\n  \n  // Login\n  loginSuccess({\n    id: '1',\n    name: 'John',\n    email: 'john@example.com',\n    // ... other fields\n  });\n  \n  // Logout\n  logout();\n  \n  // Update profile\n  updateUser({\n    ...user,\n    name: 'Jane',\n  });\n  \n  // Manage addresses\n  addAddress({\n    id: 'addr1',\n    name: 'Home',\n    street: '123 Main St',\n    city: 'New York',\n    zipCode: '10001',\n  });\n};\n\n// ============================================\n// 4. HOW TO NAVIGATE BETWEEN SCREENS\n// ============================================\n\nimport { useNavigation } from '@react-navigation/native';\n\nconst MyComponent = () => {\n  const navigation = useNavigation();\n  \n  // Navigate to another tab\n  navigation.navigate('Home'); // MainTabs name\n  navigation.navigate('Search');\n  navigation.navigate('Cart');\n  \n  // Navigate to stack screen\n  navigation.navigate('ProductDetail', { productId: '123' });\n  navigation.navigate('Checkout');\n  navigation.navigate('OrderHistory');\n  navigation.navigate('OrderDetails', { orderId: 'ORD-001' });\n  navigation.navigate('Settings');\n  \n  // Go back\n  navigation.goBack();\n  \n  // Reset to tab\n  navigation.reset({\n    index: 0,\n    routes: [{ name: 'Home' }],\n  });\n};\n\n// ============================================\n// 5. HOW TO USE VALIDATION UTILS\n// ============================================\n\nimport { validateEmail, validatePassword, validatePhone, validateForm } from '../utils/validation';\n\n// Validate single field\nconst emailResult = validateEmail('user@example.com');\nif (!emailResult.isValid) {\n  console.log(emailResult.error); // Shows error message\n}\n\n// Validate entire form\nconst formData = {\n  email: 'user@example.com',\n  password: 'password123',\n  phone: '0912345678',\n};\n\nconst rules = {\n  email: { validator: validateEmail },\n  password: { validator: validatePassword },\n  phone: { validator: validatePhone },\n};\n\nconst validation = validateForm(formData, rules);\nif (!validation.isValid) {\n  console.log(validation.errors); // {email: 'error', password: 'error', ...}\n}\n\n// ============================================\n// 6. HOW TO FORMAT DATA\n// ============================================\n\nimport {\n  formatPrice,\n  formatDate,\n  formatPhoneNumber,\n  calculateDiscount,\n  pluralize,\n  getInitials,\n} from '../utils/formatting';\n\n// Format currency\nconst priceText = formatPrice(1500000); // \"1.500.000đ\"\n\n// Format date\nconst dateText = formatDate(new Date()); // \"25/01/2026\"\n\n// Format phone\nconst phoneText = formatPhoneNumber('0912345678'); // \"0912 345 678\"\n\n// Calculate discount\nconst discountedPrice = calculateDiscount(1000, 20); // 800 (20% off)\n\n// Pluralize\nconst text = pluralize('product', 5); // \"products\"\nconst text2 = pluralize('product', 1); // \"product\"\n\n// Get initials for avatar\nconst initials = getInitials('John Doe'); // \"JD\"\n\n// ============================================\n// 7. HOW TO CREATE A NEW SCREEN\n// ============================================\n\n// File: src/screens/NewScreen.tsx\n\nimport React, { useState, useEffect } from 'react';\nimport { View, Text, StyleSheet, SafeAreaView, ScrollView, TouchableOpacity } from 'react-native';\nimport { useNavigation } from '@react-navigation/native';\nimport { COLORS, SPACING, FONT_SIZES } from '../constants';\nimport { useCart, useFavorites, useUser } from '../state/*Context';\nimport CustomButton from '../components/CustomButton';\n\nconst NewScreen: React.FC = () => {\n  // State\n  const [data, setData] = useState([]);\n  const [isLoading, setIsLoading] = useState(false);\n  \n  // Hooks\n  const navigation = useNavigation();\n  const { items } = useCart();\n  \n  // Effects\n  useEffect(() => {\n    loadData();\n  }, []);\n  \n  // Handlers\n  const loadData = async () => {\n    setIsLoading(true);\n    try {\n      // Load data\n    } catch (error) {\n      console.error('Error:', error);\n    } finally {\n      setIsLoading(false);\n    }\n  };\n  \n  const handlePress = () => {\n    navigation.navigate('AnotherScreen');\n  };\n  \n  // Render\n  return (\n    <SafeAreaView style={styles.container}>\n      <ScrollView showsVerticalScrollIndicator={false}>\n        <Text style={styles.title}>Welcome to New Screen</Text>\n        <CustomButton title=\"Go Back\" onPress={() => navigation.goBack()} />\n      </ScrollView>\n    </SafeAreaView>\n  );\n};\n\nconst styles = StyleSheet.create({\n  container: {\n    flex: 1,\n    backgroundColor: COLORS.background,\n  },\n  title: {\n    fontSize: FONT_SIZES.xl,\n    fontWeight: '700',\n    color: COLORS.text,\n    marginBottom: SPACING.lg,\n  },\n});\n\nexport default NewScreen;\n\n// Then add to src/navigation/RootNavigator.tsx:\n// <Stack.Screen name=\"NewScreen\" component={NewScreen} />\n\n// ============================================\n// 8. HOW TO CALL API\n// ============================================\n\nimport { getProducts, getProductDetail, searchProducts } from '../services/api';\n\n// Get all products\nconst products = await getProducts(1); // page 1\n\n// Get product detail\nconst product = await getProductDetail('product-123');\n\n// Search products\nconst results = await searchProducts('iPhone', 1);\n\n// Always wrap in try-catch for production:\nconst loadData = async () => {\n  try {\n    const products = await getProducts(1);\n    setProducts(products);\n  } catch (error) {\n    console.error('Error loading products:', error);\n    Alert.alert('Error', 'Could not load products');\n  }\n};\n\n// ============================================\n// 9. HOW TO USE CUSTOM COMPONENTS\n// ============================================\n\n// CustomButton\n<CustomButton\n  title=\"Click Me\"\n  variant=\"primary\"     // 'primary' | 'secondary' | 'outline'\n  size=\"large\"          // 'small' | 'medium' | 'large'\n  onPress={handlePress}\n  loading={isLoading}    // Shows spinner\n  disabled={isDisabled}  // Grays out\n  icon=\"shopping-cart\"  // Icon name\n/>\n\n// CustomInput\n<CustomInput\n  placeholder=\"Enter email\"\n  value={email}\n  onChangeText={setEmail}\n  label=\"Email\"\n  error={emailError}     // Error message\n  helperText=\"example@mail.com\"\n  icon=\"mail\"\n  disabled={false}\n/>\n\n// ProductCard\n<ProductCard\n  product={productData}\n  variant=\"grid\"        // 'grid' | 'list'\n  onPress={() => navigation.navigate('ProductDetail', { productId: product.id })}\n  onAddToCart={addToCart}\n  onToggleFavorite={toggleFavorite}\n/>\n\n// ============================================\n// 10. HOW TO HANDLE FORMS\n// ============================================\n\nconst [formData, setFormData] = useState({\n  email: '',\n  password: '',\n  phone: '',\n});\nconst [errors, setErrors] = useState({});\n\nconst handleInputChange = (field: string, value: string) => {\n  setFormData(prev => ({ ...prev, [field]: value }));\n};\n\nconst handleSubmit = () => {\n  // Validate\n  const validation = validateForm(formData, {\n    email: { validator: validateEmail },\n    password: { validator: validatePassword },\n    phone: { validator: validatePhone },\n  });\n  \n  if (!validation.isValid) {\n    setErrors(validation.errors);\n    return;\n  }\n  \n  // Proceed\n  setErrors({});\n  // Make API call or dispatch action\n};\n\nreturn (\n  <>\n    <CustomInput\n      placeholder=\"Email\"\n      value={formData.email}\n      onChangeText={(val) => handleInputChange('email', val)}\n      error={errors.email}\n    />\n    <CustomButton\n      title=\"Submit\"\n      onPress={handleSubmit}\n      disabled={Object.keys(errors).length > 0}\n    />\n  </>\n);\n\n// ============================================\n// 11. COMMON PATTERNS\n// ============================================\n\n// Loading state\nreturn isLoading ? (\n  <ActivityIndicator size=\"large\" color={COLORS.primary} />\n) : (\n  // Content\n);\n\n// Empty state\nreturn items.length === 0 ? (\n  <View style={styles.emptyContainer}>\n    <Text>No items found</Text>\n  </View>\n) : (\n  // List\n);\n\n// Error handling\ntry {\n  const data = await fetchData();\n  setData(data);\n} catch (error) {\n  Alert.alert('Error', error.message);\n}\n\n// Conditional rendering\n{user && <UserProfile user={user} />}\n{isAuthenticated ? <LogoutButton /> : <LoginButton />}\n\n// Array operations\nconst filtered = items.filter(item => item.price > 100);\nconst mapped = items.map(item => item.name);\nconst found = items.find(item => item.id === '123');\nconst total = items.reduce((sum, item) => sum + item.price, 0);\n\n// ============================================\n// 12. DEBUGGING TIPS\n// ============================================\n\n// Print state\nconsole.log('Cart state:', state);\n\n// Check if value exists\nif (!product) console.warn('Product not found');\n\n// Use debugger (breakpoint)\ndebugger;\n\n// Network requests logging\n// Add to src/services/api.ts:\nif (__DEV__) {\n  console.log('API Call:', url, options);\n}\n\n// React DevTools\n// Install React DevTools extension for browser/VS Code\n\n// React Native Debugger\n// Download from: https://github.com/jhen0409/react-native-debugger\n\n// ============================================\n// 13. PERFORMANCE TIPS\n// ============================================\n\n// Use FlatList instead of map for lists\n<FlatList\n  data={items}\n  renderItem={({item}) => <Item item={item} />}\n  keyExtractor={(item) => item.id}\n  numColumns={2}\n/>\n\n// Memoize expensive computations\nconst total = useMemo(() => {\n  return items.reduce((sum, item) => sum + item.price, 0);\n}, [items]);\n\n// Optimize re-renders\nconst handlePress = useCallback(() => {\n  // Handler code\n}, [dependencies]);\n\n// Lazy load images\n<Image\n  source={{uri: imageUrl}}\n  style={{width: 100, height: 100}}\n  onLoad={() => console.log('Image loaded')}\n/>\n\n// ============================================\n// 14. COMMON ERRORS & FIXES\n// ============================================\n\n// Error: \"useCart must be used inside CartProvider\"\nFIX: Make sure component is wrapped by CartProvider in App.tsx\n\n// Error: \"Can't find variable: product\"\nFIX: Check prop name spelling, ensure prop is passed correctly\n\n// Error: \"Cannot read property of undefined\"\nFIX: Add null/undefined checks before accessing properties\nif (!user) return null;\nconsole.log(user.name); // Safe now\n\n// Error: \"Key prop not specified\"\nFIX: Add key to list items\nitems.map(item => <Item key={item.id} item={item} />)\n\n// Error: \"Navigation.navigate is not defined\"\nFIX: Import useNavigation from @react-navigation/native\nconst navigation = useNavigation();\n\n// ============================================\n// 15. MOBILE-SPECIFIC CONSIDERATIONS\n// ============================================\n\n// Handle safe area (notches)\nimport { SafeAreaView } from 'react-native-safe-area-context';\n<SafeAreaView style={styles.container}>\n  {/* Content */}\n</SafeAreaView>\n\n// Handle keyboard\nimport { KeyboardAvoidingView, Platform } from 'react-native';\n<KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : 'height'}>\n  {/* Form fields */}\n</KeyboardAvoidingView>\n\n// Handle orientation changes\nconst [orientation, setOrientation] = useState('portrait');\nuseEffect(() => {\n  const subscription = Dimensions.addEventListener('change', ({window}) => {\n    setOrientation(window.width > window.height ? 'landscape' : 'portrait');\n  });\n  return () => subscription.remove();\n}, []);\n\n// Responsive sizing\nconst {width, height} = Dimensions.get('window');\nconst imageSize = width * 0.8; // 80% of screen width\n\n*/\n